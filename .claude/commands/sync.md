Run the sync-to-device script to deploy the current codebase from E:/dev/ultramidi to the device at G:/.

Run `python sync_to_device.py --once` from the project root. Report what was synced and the remaining free space on the device.
