Estimate the flash storage usage of the project. The device has 2 MB total flash.

Calculate the size of all files that would be deployed to the device (everything except .git/, .claude/, .vscode/, docs/, sync_to_device.py, sync_config.ini, __pycache__/).

Break down by category:
- Firmware (.py files)
- Libraries (lib/)
- Fonts (fonts/)
- Wallpapers (wallpaper/)
- Config files (ultrasetup/)

Show total used, remaining, and percentage. Flag if getting close to the 2 MB limit.
